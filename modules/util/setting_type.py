"""
A module containing the SettingType enum.
"""

from enum import Enum


class SettingType(Enum):
    """
    An enum representing the different settings that can be changed.
    """
    MUSIC = "Music"
    SOUNDS = "Sounds"
